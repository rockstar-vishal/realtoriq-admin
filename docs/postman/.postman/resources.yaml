# Use this workspace to collaborate
workspace:
  id: dc444004-6353-4891-8895-98cd4a66b6ce

localResources:
  collections:
    - ../RealtorIQ.postman_collection.json
  environments:
    - ../RealtorIQ.local.postman_environment.json

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../postman/collections/RealtorIQ — Staging deployment checks: ea50303d-aed2-4dea-a427-c63218cf838c
