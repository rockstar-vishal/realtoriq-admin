{
  "info": {
    "_postman_id": "b7f1e4a2-9c3d-4f8a-b512-6a0d9e4c1f77",
    "name": "RealtorIQ \u2014 Broker API v1",
    "description": "The JSON API the React broker app talks to.\n\n**The flow chains itself.** Run the Auth folder top to bottom: `Request sign-in code` saves `request_id`, `Verify code` saves `access_token` and `refresh_token`, and every other request picks the token up from collection auth. Nothing needs copy-pasting.\n\n**Prerequisites**\n\n1. `bin/rails db:seed` \u2014 reference data\n2. `bin/rails demo:seed` \u2014 a firm you can sign into (Sethi Realty)\n3. `bin/dev` \u2014 the server on :3000\n\n**Sign-in codes are `888888` in development.** Nothing is delivered. That is a development-only bypass; the app refuses to boot in production with it set.\n\n**Error shape.** Every failure is `{ \"error\": { \"code\", \"message\", \"details\" } }`. Switch on `code`, never on the prose. The codes are: `not_registered`, `account_disabled`, `otp_locked`, `invalid_code`, `account_suspended`, `subscription_lapsed`, `device_limit_reached`, `forbidden_role`, `rate_limited`, `delivery_failed`, `unauthorized`, `not_found`, `invalid_request`.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "api_base",
      "value": "{{base_url}}/api/v1",
      "type": "string"
    },
    {
      "key": "super_admin_mobile",
      "value": "9820144210",
      "type": "string"
    },
    {
      "key": "manager_mobile",
      "value": "9820144211",
      "type": "string"
    },
    {
      "key": "agent_mobile",
      "value": "9820144212",
      "type": "string"
    },
    {
      "key": "otp_code",
      "value": "888888",
      "type": "string"
    },
    {
      "key": "request_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "access_token",
      "value": "",
      "type": "string"
    },
    {
      "key": "refresh_token",
      "value": "",
      "type": "string"
    },
    {
      "key": "channel_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "agent_access_token",
      "value": "",
      "type": "string"
    },
    {
      "key": "lead_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "created_user_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "property_type_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "typology_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "signed_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "city_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "locality_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "builder_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "building_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "project_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "property_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "booking_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "invoice_id",
      "value": "",
      "type": "string"
    },
    {
      "key": "user_id",
      "value": ""
    },
    {
      "key": "document_id",
      "value": ""
    },
    {
      "key": "project_photo_id",
      "value": ""
    },
    {
      "key": "property_photo_id",
      "value": ""
    },
    {
      "key": "brochure_signed_id",
      "value": ""
    },
    {
      "key": "lead_project_id",
      "value": "",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Auth",
      "description": "No passwords and no self-signup. Ops create the account; possession of the mobile is the credential.",
      "item": [
        {
          "name": "1. Request sign-in code",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/otp",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "otp"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"mobile\": \"{{super_admin_mobile}}\"\n}"
            },
            "description": "Accepts any format of the number \u2014 `9820144210`, `+91 98201 44210`, `098201 44210` all resolve to the same user, because mobiles are normalised to E.164 and are globally unique.\n\nSaves `request_id` for the next request."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "pm.collectionVariables.set(\"request_id\", body.request_id);",
                  "",
                  "pm.test(\"returns a request_id to verify against\", () => {",
                  "  pm.expect(body.request_id).to.be.a(\"string\").and.not.empty;",
                  "});",
                  "",
                  "pm.test(\"masks the destination, for the OTP screen's copy\", () => {",
                  "  pm.expect(body.sent_to).to.include(\"xxx\");",
                  "});",
                  "",
                  "pm.test(\"code has not already expired\", () => {",
                  "  pm.expect(body.expires_in).to.be.above(0);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "2. Verify code and sign in",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/verify",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"request_id\": \"{{request_id}}\",\n  \"code\": \"{{otp_code}}\",\n  \"device\": {\n    \"device_id\": \"postman-1\",\n    \"device_name\": \"Postman\",\n    \"platform\": \"web\",\n    \"app_version\": \"dev\"\n  }\n}"
            },
            "description": "`device` is optional but recommended. `device_id` identifies the physical device, so reinstalling reclaims that slot rather than consuming another. Past the plan's device limit the **oldest** session is signed out and the newest device gets in.\n\nSaves `access_token` and `refresh_token`."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "pm.collectionVariables.set(\"access_token\", body.access_token);",
                  "pm.collectionVariables.set(\"refresh_token\", body.refresh_token);",
                  "",
                  "pm.test(\"returns both tokens\", () => {",
                  "  pm.expect(body.access_token).to.be.a(\"string\").and.not.empty;",
                  "  pm.expect(body.refresh_token).to.be.a(\"string\").and.not.empty;",
                  "});",
                  "",
                  "pm.test(\"access token is short-lived\", () => {",
                  "  pm.expect(body.expires_in).to.eql(900);",
                  "});",
                  "",
                  "pm.test(\"carries the firm and its entitlement\", () => {",
                  "  pm.expect(body.firm.code).to.match(/^CP-/);",
                  "  pm.expect(body.subscription.entitled).to.be.true;",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "3. Refresh access token",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/refresh",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "refresh"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refresh_token\": \"{{refresh_token}}\"\n}"
            },
            "description": "Refresh tokens **rotate on every use**: a stolen token is good for exactly one call, and using it invalidates the copy the real device holds \u2014 which surfaces the theft rather than hiding it.\n\nRun this twice in a row and the second attempt returns 401. That is the design working, not a bug."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "const previous = pm.collectionVariables.get(\"refresh_token\");",
                  "",
                  "pm.test(\"refresh token rotated\", () => {",
                  "  pm.expect(body.refresh_token).to.not.eql(previous);",
                  "});",
                  "",
                  "pm.collectionVariables.set(\"access_token\", body.access_token);",
                  "pm.collectionVariables.set(\"refresh_token\", body.refresh_token);"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Session",
      "item": [
        {
          "name": "Who am I",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/me",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "me"
              ]
            },
            "description": "What the app loads at boot: the user, their firm, the subscription, and **permissions resolved server-side** so the client never reimplements the role rules.\n\n`permissions.manage_users` drives the team screen. `permissions.manage_projects` drives My Projects create/edit and inline builder create.\n\nThis is also where the blocked states surface \u2014 suspend the firm in the admin panel and this returns 403 `account_suspended`; let the subscription lapse and it returns 402 `subscription_lapsed`."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "pm.test(\"resolves permissions server-side\", () => {",
                  "  pm.expect(body.permissions).to.have.property(\"verify_contact_channels\");",
                  "  pm.expect(body.permissions).to.have.property(\"manage_users\");",
                  "  pm.expect(body.permissions).to.have.property(\"manage_projects\");",
                  "});",
                  "",
                  "pm.test(\"reports the device limit from the plan\", () => {",
                  "  pm.expect(body.limits.devices).to.be.a(\"number\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Reference data",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/reference",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "reference"
              ]
            },
            "description": "Every dropdown the app needs, in one request: cities, localities, builders, typologies, lead sources, lead statuses, property types, plus the fixed transaction-type and floor-band enums.\n\nGlobal data, identical for every firm, so it carries an **ETag**. Send `If-None-Match` and you get a 304 instead of the payload \u2014 cheap to revalidate on every app launch.\n\nNote `lead_statuses[].is_dead` / `is_booked`: those flags are what make the dead-leads and bookings reports computable without hardcoding status names."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "pm.test(\"carries every master the app needs\", () => {",
                  "  [\"cities\", \"localities\", \"builders\", \"typologies\",",
                  "   \"lead_sources\", \"lead_statuses\", \"property_types\"].forEach((key) => {",
                  "    pm.expect(body[key], key).to.be.an(\"array\").that.is.not.empty;",
                  "  });",
                  "});",
                  "",
                  "pm.test(\"lead statuses carry the report flags\", () => {",
                  "  pm.expect(body.lead_statuses.some((s) => s.is_dead)).to.be.true;",
                  "  pm.expect(body.lead_statuses.some((s) => s.is_booked)).to.be.true;",
                  "});",
                  "",
                  "pm.test(\"is cacheable\", () => {",
                  "  // Header lookup is case-sensitive in Newman, and Puma emits `etag`.",
                  "  const etag = pm.response.headers.get(\"ETag\") || pm.response.headers.get(\"etag\");",
                  "  pm.expect(etag).to.be.a(\"string\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Dashboard \u2014 the whole home screen",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/dashboard",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "dashboard"
              ]
            },
            "description": "The whole home screen in one request \u2014 the lead pipeline counters, the money tiles, the inventory strip, and the top three of each list.\n\nIt exists because every counter here is derivable from a filter the API already exposes, so the alternative was six round trips for one card.\n\nHome **Your pipeline** tiles read `total`, `new`, `todays_followups` (label: Today's Calls), `missed_followups`, `visit_planned`, `hot_negotiation`. `hot`, `visited` and `bookings` stay on the payload. This is not `GET /leads` `counts`.\n\n`leads.recent` is at most three **missed followups**, most overdue first \u2014 not the worklist.\n\n**The `money` block is absent for an agent**, not zeroed. Agents get `forbidden_role` on every booking endpoint, and a block full of zeroes would read as \"no revenue\" \u2014 a different and wrong statement. Branch on the key being present.\n\n`this_fy` is the **Indian financial year**, 1 April to 31 March. A 31 March booking and a 1 April one fall in different years."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const b = pm.response.json();",
                  "",
                  "pm.test(\"the lead counters are all there\", () => {",
                  "  [\"total\",\"new\",\"hot\",\"hot_negotiation\",\"todays_followups\",\"missed_followups\",\"visit_planned\",\"visited\",\"bookings\"].forEach(k => {",
                  "    pm.expect(b.leads[k], k).to.be.a(\"number\");",
                  "  });",
                  "});",
                  "pm.test(\"the missed-followup strip is at most three leads\", () => {",
                  "  pm.expect(b.leads.recent.length).to.be.at.most(3);",
                  "});",
                  "",
                  "pm.test(\"the money tiles are all there\", () => {",
                  "  [\"revenue_till_date\",\"brokerage_earned\",\"bookings_count\",\"invoiced\",\"collected\",\"outstanding\"]",
                  "    .forEach(k => pm.expect(b.money[k], k).to.be.a(\"number\"));",
                  "});",
                  "",
                  "// The arithmetic the client must never redo itself.",
                  "pm.test(\"outstanding is invoiced minus collected\", () => {",
                  "  pm.expect(b.money.outstanding).to.eql(b.money.invoiced - b.money.collected);",
                  "});",
                  "pm.test(\"brokerage never exceeds revenue\", () => {",
                  "  pm.expect(b.money.brokerage_earned).to.be.at.most(b.money.revenue_till_date);",
                  "});",
                  "pm.test(\"this month is within this FY\", () => {",
                  "  pm.expect(b.money.this_month.revenue).to.be.at.most(b.money.this_fy.revenue);",
                  "});",
                  "",
                  "// 1 April to 31 March, not the calendar year.",
                  "pm.test(\"the FY window is Indian, and labelled the way a broker writes it\", () => {",
                  "  pm.expect(b.money.this_fy.starts_on).to.match(/-04-01$/);",
                  "  pm.expect(b.money.this_fy.ends_on).to.match(/-03-31$/);",
                  "  pm.expect(b.money.this_fy.label).to.match(/^\\d{4}-\\d{2}$/);",
                  "});",
                  "",
                  "pm.test(\"cancelled bookings are reported apart from revenue\", () => {",
                  "  pm.expect(b.money.cancelled.count).to.be.a(\"number\");",
                  "});",
                  "pm.test(\"the inventory strip is there\", () => {",
                  "  pm.expect(b.inventory.properties).to.be.a(\"number\");",
                  "  pm.expect(b.inventory.properties_added_this_week).to.be.at.most(b.inventory.properties);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Notifications",
      "description": "In-app inbox and browser push for the signed-in session. A test push goes only to this device. Console: Notifications::SendTest.to_user(user) reaches every device that broker registered.",
      "item": [
        {
          "name": "List notifications",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{api_base}}/notifications"
          }
        },
        {
          "name": "Unread count",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{api_base}}/notifications/unread_count"
          }
        },
        {
          "name": "Mark all read",
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{api_base}}/notifications/mark_all_read"
          }
        },
        {
          "name": "This device's push subscription",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{api_base}}/push_subscriptions"
          }
        },
        {
          "name": "VAPID public key",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{api_base}}/push_subscriptions/vapid_public_key"
          }
        },
        {
          "name": "Send test push",
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{api_base}}/notifications/test",
            "description": "201 means the push service accepted the message for this session only. 422 no_subscription means this browser was never registered."
          }
        }
      ]
    },
    {
      "name": "Firm contact channels",
      "description": "The firm's email, mobile and WhatsApp \u2014 the three rows behind Settings \u2192 Contact channels.\n\nThese belong to the **firm**, not to individual users. A user's own mobile receives the login code, but that is authentication (proving possession at sign-in), not channel verification.\n\nAny user can read them; only the firm's `super_admin` can act on them.",
      "item": [
        {
          "name": "List channels",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/firm/contact_channels",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "firm",
                "contact_channels"
              ]
            },
            "description": "`can_verify` tells the client whether to render the verify actions at all.\n\nSaves an **unverified** channel's id for the next two requests \u2014 `demo:seed` leaves WhatsApp unverified on purpose so the flow is walkable on a fresh database."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "// Prefer an unverified channel: sending a code to an already-verified",
                  "// one is refused, which would make a second collection run fail.",
                  "const target = body.channels.find((c) => !c.verified) || body.channels[0];",
                  "if (target) pm.collectionVariables.set(\"channel_id\", target.id);",
                  "",
                  "pm.test(\"returns one channel of each kind\", () => {",
                  "  pm.expect(body.channels.map((c) => c.kind).sort())",
                  "    .to.eql([\"email\", \"mobile\", \"whatsapp\"]);",
                  "});",
                  "",
                  "pm.test(\"tells the client whether this user may verify\", () => {",
                  "  pm.expect(body.can_verify).to.be.a(\"boolean\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Send verification code",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/firm/contact_channels/{{channel_id}}/request_code",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "firm",
                "contact_channels",
                "{{channel_id}}",
                "request_code"
              ]
            },
            "description": "Routes by channel kind: email through Action Mailer, mobile and WhatsApp through MSG91.\n\nThere is a 60-second resend cooldown \u2014 call it twice quickly and the second returns 422 `code_not_sent`. An already-verified channel is refused the same way, which is why the assertions below accept both outcomes: it keeps repeat runs honest instead of green-by-luck."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"code sent, or refused for a stated reason\", () => {",
                  "  pm.expect([200, 422]).to.include(pm.response.code);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "  pm.test(\"channel moves to pending\", () => {",
                  "    pm.expect(pm.response.json().channel.state).to.eql(\"pending\");",
                  "  });",
                  "} else {",
                  "  pm.test(\"refusal is typed\", () => {",
                  "    pm.expect(pm.response.json().error.code).to.eql(\"code_not_sent\");",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Verify channel",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/firm/contact_channels/{{channel_id}}/verify",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "firm",
                "contact_channels",
                "{{channel_id}}",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"code\": \"{{otp_code}}\"\n}"
            },
            "description": "Records **who** verified it, so the admin panel can show \"verified by Tanmay Sethi\" rather than just a green badge.\n\nA wrong code returns 422 `invalid_code` with `details.attempts_left`; the channel fails after five.\n\nRe-run `bin/rails demo:seed` to put the demo firm's WhatsApp channel back to unverified."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"verified, or no live code to verify against\", () => {",
                  "  pm.expect([200, 422]).to.include(pm.response.code);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "  pm.test(\"channel is verified, and by whom\", () => {",
                  "    const channel = pm.response.json().channel;",
                  "    pm.expect(channel.verified).to.be.true;",
                  "    pm.expect(channel.verified_at).to.not.be.null;",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Users",
      "description": "Ops create the firm and its one super admin. That person creates everyone else \u2014 no invite SMS.\n\n`GET /users` is also the reassignment picker. The super admin sees the whole firm (including disabled). Everyone else sees **active manageables**: themselves plus anyone who reports to them.\n\nA person may have several managers. Super admins are not in that graph. A cycle is `422 reporting_cycle`.",
      "item": [
        {
          "name": "List users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/users",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "users"
              ]
            },
            "description": "Picker and team screen. Saves the first non-super-admin id as `created_user_id` for the requests below."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const users = pm.response.json().users || [];",
                  "pm.test(\"returns an array\", () => pm.expect(users).to.be.an(\"array\"));",
                  "",
                  "const other = users.find(u => u.role !== \"super_admin\");",
                  "if (other) { pm.collectionVariables.set(\"created_user_id\", other.id); }"
                ]
              }
            }
          ]
        },
        {
          "name": "Create an agent",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/users",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "users"
              ]
            },
            "description": "Super admin only. `role` is `manager` or `agent`. `manager_ids` is an array. The new user can sign in immediately.\n\n`422 user_limit_reached` when the plan is full \u2014 disabled accounts still occupy a seat.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Ananya Iyer\",\n  \"mobile\": \"9820199001\",\n  \"email\": \"ananya.iyer@example.com\",\n  \"role\": \"agent\",\n  \"manager_ids\": [\n    \"{{user_id}}\"\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/me\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200) { pm.collectionVariables.set(\"user_id\", res.json().user.id); }",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"created, or already exists / over limit\", () => {",
                  "  pm.expect([201, 422]).to.include(pm.response.code);",
                  "});",
                  "",
                  "if (pm.response.code === 201) {",
                  "  pm.collectionVariables.set(\"created_user_id\", pm.response.json().user.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Add a reporting line",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/users/{{created_user_id}}/managers",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "users",
                "{{created_user_id}}",
                "managers"
              ]
            },
            "description": "Super admin only. A cycle is `422 reporting_cycle`. The super admin cannot be a manager in this table.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"manager_id\": \"{{user_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"created, already exists, or would cycle\", () => {",
                  "  pm.expect([201, 422, 404]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Disable a user",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/users/{{created_user_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "users",
                "{{created_user_id}}"
              ]
            },
            "description": "Revokes every live session. Reversible. The firm's super admin cannot be disabled.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"disabled\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 or 422 (super admin cannot be disabled)\", () => {",
                  "  pm.expect([200, 422, 404]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Leads",
      "description": "The core CRM object.\n\n**Visibility is enforced server-side.** An agent sees only leads assigned to them; managers and the super admin see the firm's whole pipeline. A lead an agent may not see returns **404, not 403** \u2014 a 403 would confirm it exists.\n\n**Write `budget` (single amount \u2192 `budget_max`).** GET `budget_min`/`budget_max` are a filter *window* on that stored amount, not range overlap.\n\n`missed_followup=true` is an overdue-NCD filter (not a status). `status=hot_negotiation` is hot+negotiation. Drawer params drop `q`; cards (`status`, `visited`, `missed_followup`) do not.\n\nList returns `counts` (pipeline totals, unfiltered). Default sort is NCD NULLS FIRST. Money is raw integer rupees; the client formats lakh/crore. Screen guide: frontend/YASH.md.",
      "item": [
        {
          "name": "List leads",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads"
              ]
            },
            "description": "Default sort is NCD: `next_action_at ASC NULLS FIRST` (no date sits above overdue). `sort=worklist` is overdue-first.\n\nFilters: `q` (dropped if any drawer param is set), `status` (`new` \u00b7 `hot_negotiation` \u00b7 codes, or `status[]=`), `visited`, `missed_followup`, drawer `name`/`mobile`/`email`/`ncd_from`/`ncd_upto`, `transaction_type`, `property_type_id`, `typology_ids[]`, `budget_min`/`budget_max` (window on stored amount), `possession_from`/`possession_to`, `source_id`, `assigned_user_id`, `sort` (`ncd` default \u00b7 `worklist` \u00b7 `recent` \u00b7 `updated`), `page`, `per_page` (max 50).\n\nResponse includes `counts`. Saves the first lead's id for the requests below."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "if (body.leads.length) pm.collectionVariables.set(\"lead_id\", body.leads[0].id);",
                  "",
                  "pm.test(\"paginated\", () => {",
                  "  pm.expect(body.meta).to.have.property(\"total_count\");",
                  "  pm.expect(body.meta.per_page).to.eql(25);",
                  "});",
                  "",
                  "pm.test(\"card counts\", () => {",
                  "  pm.expect(body.counts).to.include.keys(\"new\", \"missed_followup\", \"visit_planned\", \"visited\", \"hot_negotiation\", \"booked\");",
                  "});",
                  "",
                  "pm.test(\"list card fields\", () => {",
                  "  if (!body.leads.length) return;",
                  "  const l = body.leads[0];",
                  "  pm.expect(l).to.have.property(\"source\");",
                  "  pm.expect(l.visit_count).to.be.a(\"number\");",
                  "  pm.expect(l).to.have.property(\"last_followup_comment\");",
                  "  pm.expect(l.created_at).to.be.a(\"string\");",
                  "  pm.expect(l).to.have.property(\"budget\");",
                  "  pm.expect(l).to.have.property(\"transaction_type\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List \u2014 missed followups only",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads?missed_followup=true",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads"
              ],
              "query": [
                {
                  "key": "missed_followup",
                  "value": "true"
                }
              ]
            },
            "description": "The design's \"Missed f/u\" tab. Not a status \u2014 `LEAD_STATUSES` has no such entry \u2014 but an overdue next action on a lead that isn't already dead or booked."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"every result really is overdue\", () => {",
                  "  pm.response.json().leads.forEach((l) => pm.expect(l.overdue).to.be.true);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List \u2014 every filter",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads?status=new&transaction_type=sale&property_type_id={{property_type_id}}&source_id={{lead_source_id}}&assigned_user_id={{user_id}}&budget_min=8000000&budget_max=25000000&possession_from=2026-01-01&possession_to=2028-12-31&typology_ids[]={{typology_id}}&ncd_from=2026-01-01&ncd_upto=2026-12-31&sort=ncd&per_page=5&page=1",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads"
              ],
              "query": [
                {
                  "key": "status",
                  "value": "new"
                },
                {
                  "key": "transaction_type",
                  "value": "sale"
                },
                {
                  "key": "property_type_id",
                  "value": "{{property_type_id}}"
                },
                {
                  "key": "source_id",
                  "value": "{{lead_source_id}}"
                },
                {
                  "key": "assigned_user_id",
                  "value": "{{user_id}}"
                },
                {
                  "key": "budget_min",
                  "value": "8000000"
                },
                {
                  "key": "budget_max",
                  "value": "25000000"
                },
                {
                  "key": "possession_from",
                  "value": "2026-01-01"
                },
                {
                  "key": "possession_to",
                  "value": "2028-12-31"
                },
                {
                  "key": "typology_ids[]",
                  "value": "{{typology_id}}"
                },
                {
                  "key": "ncd_from",
                  "value": "2026-01-01"
                },
                {
                  "key": "ncd_upto",
                  "value": "2026-12-31"
                },
                {
                  "key": "sort",
                  "value": "ncd"
                },
                {
                  "key": "per_page",
                  "value": "5"
                },
                {
                  "key": "page",
                  "value": "1"
                }
              ]
            },
            "description": "Every filter the leads index accepts, in one request \u2014 this is the reference for what is queryable.\n\nDrawer params are sent without `q` because a drawer field drops `q`. GET `budget_min`/`budget_max` are a window on the stored amount (`budget`), not range overlap. `ncd_from`/`ncd_upto` are inclusive IST days.\n\n`missed_followup=true` is not a real status \u2014 `next_action_at <= now` on a non-terminal lead. `status=hot_negotiation` is hot+negotiation.\n\n`sort` is `ncd` (default, NULLS FIRST), `worklist`, `recent` or `updated`. `typology_ids` repeats as `typology_ids[]=`. `per_page` is clamped to 1..50; absent or 0 means 25. Response includes `counts` (unfiltered pipeline totals)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "// Filters legitimately narrow to zero rows. What must hold is that every",
                  "// parameter was understood \u2014 an unknown one is ignored in Rails, so a 200",
                  "// with sane pagination is the honest assertion here.",
                  "pm.test(\"pagination meta comes back\", () => {",
                  "  pm.expect(body.meta.page).to.eql(1);",
                  "  pm.expect(body.meta.per_page).to.eql(5);",
                  "  pm.expect(body.meta.total_count).to.be.a(\"number\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Create a lead",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads"
              ]
            },
            "description": "Every field the endpoint accepts, in one body.\n\n**One lead per (firm, mobile, transaction_type).** A second sale on the same number is `422 duplicate_lead` with `details.lead_id`. Sale + rent on one number is allowed.\n\n`notes` is Client's requirements. Optional `project_id` copies blank budget/possession/notes from that project and maps it.\n\n**`property_type_id` is a single string, not an array.** It is permitted as a scalar, and Rails' `params.permit` *silently discards* an array value for a scalar key \u2014 so sending `[\"01a0\u2026\"]` arrives as nil and you get `422 Property type is required for a sale lead` with nothing pointing at the real cause. `typology_ids` **is** an array: one property type, many configurations.\n\n**The date field is `possession_by`.** `possession_up_to` and similar near-misses are dropped in the same silent way \u2014 you get a `201` and a lead with no possession date.\n\n`mobile` is normalised, so \"97597 54343\" and \"+919759754343\" are the same lead.\n\n`property_type_id` is required for `sale` and forbidden for `rent`.\n\nNCD and the opening comment are nested: `followup: { comment, next_action_at }`. Top-level `next_action_at` / `next_action_note` are ignored. `notes` is Detailed Client Requirements.\n\nNCD and the opening comment are nested: `followup: { comment, next_action_at }`. Top-level `next_action_at` / `next_action_note` are ignored. `notes` is Detailed Client Requirements.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Yash Raheja\",\n  \"mobile\": \"97597 54343\",\n  \"alt_mobile\": \"+919820144299\",\n  \"email\": \"yash.raheja@example.com\",\n  \"transaction_type\": \"sale\",\n  \"property_type_id\": \"{{property_type_id}}\",\n  \"budget\": 18000000,\n  \"possession_by\": \"2027-06-30\",\n  \"lead_source_id\": \"{{lead_source_id}}\",\n  \"source_detail\": \"99acres enquiry #48213\",\n  \"assigned_user_id\": \"{{user_id}}\",\n  \"followup\": {\n    \"comment\": \"Call back after site visit\",\n    \"next_action_at\": \"2026-09-02T10:30:00+05:30\"\n  },\n  \"notes\": \"Wants a higher floor, no ground.\",\n  \"typology_ids\": [\n    \"{{typology_id}}\"\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Several bodies below reference the signed-in user; fetch the id once.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/me\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200) { pm.collectionVariables.set(\"user_id\", res.json().user.id); }",
                  "});",
                  "",
                  "// Grab real master ids so the body doesn't hardcode uuids.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/reference\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 200) { return; }",
                  "  const ref = res.json();",
                  "  const underConstruction = ref.property_types.find((p) => /construction/i.test(p.name));",
                  "  pm.collectionVariables.set(\"property_type_id\", (underConstruction || ref.property_types[0]).id);",
                  "  pm.collectionVariables.set(\"typology_id\", ref.typologies[0].id);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const body = pm.response.json();",
                  "pm.collectionVariables.set(\"lead_id\", body.lead.id);",
                  "",
                  "pm.test(\"gets a per-firm sequential code\", () => {",
                  "  pm.expect(body.lead.code).to.match(/^L-\\d{4,}$/);",
                  "});",
                  "",
                  "pm.test(\"opens its status history\", () => {",
                  "  pm.expect(body.lead.status_history.length).to.be.at.least(1);",
                  "});",
                  "",
                  "pm.test(\"reports duplicates rather than refusing\", () => {",
                  "  pm.expect(body.possible_duplicates).to.be.an(\"array\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get a lead",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}"
              ]
            },
            "description": "Detail payload: the lead, mappings (`mapped_projects`, `mapped_properties`), its recent timeline and its full status history.\n\nJoin ids on `mapped_*[].id` are what DELETE /leads/:id/projects/:id and /properties/:id take."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"carries the timeline, history and mappings\", () => {",
                  "  const lead = pm.response.json().lead;",
                  "  pm.expect(lead.activities).to.be.an(\"array\");",
                  "  pm.expect(lead.status_history).to.be.an(\"array\");",
                  "  pm.expect(lead.mapped_projects).to.be.an(\"array\");",
                  "  pm.expect(lead.mapped_properties).to.be.an(\"array\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Map a project onto a lead",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/projects",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "projects"
              ]
            },
            "description": "Anyone who can see the lead. Body is `{ project_id }` \u2014 own or catalog. Returns the refreshed lead. Join id is `lead.mapped_projects[].id`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"project_id\": \"{{project_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/projects?per_page=1\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200 && res.json().projects.length) {",
                  "    pm.collectionVariables.set(\"project_id\", res.json().projects[0].id);",
                  "  }",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "const lead = pm.response.json().lead;",
                  "if (lead.mapped_projects.length) {",
                  "  pm.collectionVariables.set(\"lead_project_id\", lead.mapped_projects[0].id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Show new matches \u2014 stub",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/matches",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "matches"
              ]
            },
            "description": "LaunchIQ is not wired. Returns `{ matches: [] }`. Do not fake cards from GET /projects."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "pm.test(\"empty until LaunchIQ\", () => {",
                  "  pm.expect(pm.response.json().matches).to.eql([]);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Unmap a project",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/projects/{{lead_project_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "projects",
                "{{lead_project_id}}"
              ]
            },
            "description": "`:id` is the join id from mapped_projects[], not the project id."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "Update a lead",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}"
              ]
            },
            "description": "Every accepted field. Send only what changed \u2014 this body sends everything to document the shape.\n\nSending `typology_ids` **replaces the whole set**; omitting the key leaves it alone. A partial list silently drops the rest.\n\n`next_action_at` / `next_action_note` are ignored on PATCH. Move NCD with POST /leads/:id/followups.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Yash Raheja\",\n  \"mobile\": \"97597 54343\",\n  \"alt_mobile\": \"+919820144288\",\n  \"email\": \"yash.r@example.com\",\n  \"transaction_type\": \"sale\",\n  \"property_type_id\": \"{{property_type_id}}\",\n  \"budget\": 19000000,\n  \"possession_by\": \"2027-12-31\",\n  \"lead_source_id\": \"{{lead_source_id}}\",\n  \"source_detail\": \"Referred by Mehta\",\n  \"assigned_user_id\": \"{{user_id}}\",\n  \"notes\": \"Budget widened after loan approval.\",\n  \"typology_ids\": [\n    \"{{typology_id}}\"\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "Reassign a lead",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}"
              ]
            },
            "description": "There is no `/assign` path. Send `assigned_user_id` on `PATCH /leads/:id`.\n\nThe super admin can assign anyone active in the firm. Anyone else is limited to their **active manageables** (themselves plus reports). `null` unassigns \u2014 manager-role+ only \u2014 and hides the lead from every agent.\n\nThe pre-request script reads the signed-in user's own id, so the body hardcodes nothing.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"assigned_user_id\": \"{{user_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Assign the lead to whoever is signed in.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/me\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200) { pm.collectionVariables.set(\"user_id\", res.json().user.id); }",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const lead = pm.response.json().lead;",
                  "pm.test(\"the lead now has an owner\", () => {",
                  "  pm.expect(lead.assigned_user.id).to.eql(pm.collectionVariables.get(\"user_id\"));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Add a followup",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/followups",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "followups"
              ]
            },
            "description": "The only write path for a follow-up comment and for moving NCD. Optional `status` runs TransitionStatus in the same transaction \u2014 do not also POST /leads/:id/status.\n\n`comment` is required. Omit `next_action_at` to leave the lead's current NCD. Dead needs `reason`; booked needs `booked_on` (application date on leads.booked_at, no booking row). Terminal statuses clear NCD.\n\nTop-level next_action_at on POST/PATCH /leads is ignored.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"comment\": \"Call back after site visit\",\n  \"next_action_at\": \"2026-09-02T10:30:00+05:30\",\n  \"status\": \"followup\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const body = pm.response.json();",
                  "pm.test(\"returns the followup and a lead card\", () => {",
                  "  pm.expect(body.followup.comment).to.eql(\"Call back after site visit\");",
                  "  pm.expect(body.lead.last_followup_comment).to.eql(\"Call back after site visit\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List followups",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/followups",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "followups"
              ]
            },
            "description": "Newest first. `meta` is the usual page / per_page / total_count / total_pages. Default 25."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "pm.test(\"newest first array\", () => {",
                  "  pm.expect(pm.response.json().followups).to.be.an(\"array\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Log a visit",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/visits",
              "host": ["{{api_base}}"],
              "path": ["leads", "{{lead_id}}", "visits"]
            },
            "description": "A completed outing. visited_on is YYYY-MM-DD (IST). Sites are optional and must already be mapped. Does not change status or NCD. Returns { visit } only.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visited_on\": \"2026-09-20\",\n  \"notes\": \"Walked the site\",\n  \"project_ids\": [],\n  \"property_ids\": []\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "const visit = pm.response.json().visit;",
                  "pm.test(\"returns the visit date\", () => {",
                  "  pm.expect(visit.visited_on).to.eql(\"2026-09-20\");",
                  "});",
                  "if (visit && visit.id) pm.collectionVariables.set(\"visit_id\", visit.id);"
                ]
              }
            }
          ]
        },
        {
          "name": "List visits",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/visits",
              "host": ["{{api_base}}"],
              "path": ["leads", "{{lead_id}}", "visits"]
            },
            "description": "Newest first. visit_count on the lead is the number of these rows."
          }
        },
        {
          "name": "Edit a visit",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/visits/{{visit_id}}",
              "host": ["{{api_base}}"],
              "path": ["leads", "{{lead_id}}", "visits", "{{visit_id}}"]
            },
            "description": "Date, notes, and sites. New site ids must be mapped. Ids already on this visit may stay after the lead unmaps them. user_id does not change.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visited_on\": \"2026-09-18\",\n  \"notes\": \"Corrected the date\",\n  \"project_ids\": [],\n  \"property_ids\": []\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "const visit = pm.response.json().visit;",
                  "pm.test(\"updates notes\", () => {",
                  "  pm.expect(visit.notes).to.eql(\"Corrected the date\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Log a call",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/activities",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "activities"
              ]
            },
            "description": "Kinds: `call`, `whatsapp`, `note`. `visit` and `status_change` are refused here. A site visit is POST /leads/:id/visits.\n\n`occurred_at` is optional and defaults to now, so a broker can log this evening the call they made this morning.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"kind\": \"call\",\n  \"body\": \"Discussed the 3 BHK at Aurum Vista. Wants a higher floor.\",\n  \"occurred_at\": \"2026-09-01T11:15:00+05:30\",\n  \"outcome\": \"interested\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));"
                ]
              }
            }
          ]
        },
        {
          "name": "kind=visit is rejected",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/activities",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "activities"
              ]
            },
            "description": "Site visits are lead_visits, not activities. kind=visit is 422.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"kind\": \"visit\",\n  \"body\": \"Site visit to Aurum Vista\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 kind=visit rejected\", () => pm.response.to.have.status(422));"
                ]
              }
            }
          ]
        },
        {
          "name": "Timeline",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/activities",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "activities"
              ]
            },
            "description": "Newest first. `system: true` marks rows the app wrote itself, so the client can render them differently from something a person typed."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "Mark dead \u2014 reason required",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/status",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "status"
              ]
            },
            "description": "Deliberately refused. A lead marked dead with no reason is a lead nobody can learn from, and the dead-leads report exists to answer \"why\".",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"dead\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 reason_required\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"reason_required\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Mark dead \u2014 with a reason",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/status",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "status"
              ]
            },
            "description": "Writes three things in one transaction: the lead's status, a `lead_status_changes` row (which is what the dead-leads report groups by month), and a timeline entry.\n\nReviving the lead afterwards clears `dead_reason` and `dead_at`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"dead\",\n  \"reason\": \"Bought through another channel partner\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"records the reason and the history\", () => {",
                  "  const lead = pm.response.json().lead;",
                  "  pm.expect(lead.dead_reason).to.be.a(\"string\");",
                  "  pm.expect(lead.dead_at).to.not.be.null;",
                  "  pm.expect(lead.status_history.length).to.be.at.least(2);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Revive it",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/leads/{{lead_id}}/status",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "leads",
                "{{lead_id}}",
                "status"
              ]
            },
            "description": "Clears the death details \u2014 otherwise a revived lead still reads as dead everywhere it's shown.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"hot\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"death details cleared\", () => {",
                  "  const lead = pm.response.json().lead;",
                  "  pm.expect(lead.dead_reason).to.be.null;",
                  "  pm.expect(lead.dead_at).to.be.null;",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Uploads",
      "description": "Direct upload: ask for a ticket, PUT the file straight to storage, then pass the returned `signed_id` when creating the record it belongs to.\n\nLimits are enforced **when the ticket is issued**, per purpose \u2014 after that the browser talks to storage and this is the last point we control. Rails' own `/rails/active_storage/direct_uploads` is shadowed with a 404, since it sits outside this auth and enforces none of these rules.",
      "item": [
        {
          "name": "Request an upload ticket",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/uploads",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "uploads"
              ]
            },
            "description": "Purposes and caps: `booking_document` 2 MB (PDF/JPG/PNG), `project_brochure` 5 MB (PDF), `project_photo` and `property_photo` 5 MB (JPG/PNG/WebP), `firm_logo` 1 MB.\n\n`checksum` is the base64 MD5 of the file \u2014 storage rejects the PUT if it doesn't match, which is the integrity check, not a bug.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"purpose\": \"project_brochure\",\n  \"filename\": \"aurum-vista.pdf\",\n  \"byte_size\": 1048576,\n  \"checksum\": \"XrY7u+Ae7tCTyyK7j1rNww==\",\n  \"content_type\": \"application/pdf\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const body = pm.response.json();",
                  "pm.collectionVariables.set(\"signed_id\", body.signed_id);",
                  "",
                  "pm.test(\"returns somewhere to PUT the file\", () => {",
                  "  pm.expect(body.direct_upload.url).to.be.a(\"string\");",
                  "  pm.expect(body.direct_upload.headers).to.be.an(\"object\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Oversize file is refused",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/uploads",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "uploads"
              ]
            },
            "description": "Booking documents are capped at 2 MB on the design's own screen.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"purpose\": \"booking_document\",\n  \"filename\": \"huge.pdf\",\n  \"byte_size\": 99999999,\n  \"checksum\": \"XrY7u+Ae7tCTyyK7j1rNww==\",\n  \"content_type\": \"application/pdf\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 file_too_large\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"file_too_large\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Wrong type is refused",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/uploads",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "uploads"
              ]
            },
            "description": "A brochure is a PDF. Each purpose carries its own allow-list.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"purpose\": \"project_brochure\",\n  \"filename\": \"photo.png\",\n  \"byte_size\": 1000,\n  \"checksum\": \"XrY7u+Ae7tCTyyK7j1rNww==\",\n  \"content_type\": \"image/png\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 unsupported_type\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"unsupported_type\");",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Inventory",
      "description": "Projects are developer stock a broker sells on commission. Properties are resale and rental listings. Buildings hold properties and are **firm-owned** \u2014 brokers create them inline from the property form, so one firm's typo never reaches another's dropdown.\n\n**Inventory is firm-wide**, unlike leads: everyone in the firm sees all of it.\n\n**Money is raw rupees.** The design's inputs read \"\u20b9 lakh\" \u2014 that is a client-side unit, converted before it gets here.\n\n**Price and area bands are derived**, not stored: min/max across a project's typologies. A stored band could disagree with its own rows.",
      "item": [
        {
          "name": "List builders",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/builders",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "builders"
              ]
            },
            "description": "The platform's curated developers plus this firm's own. `global: false` marks the ones the firm added \u2014 those are theirs to correct; the global list is not."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const builders = pm.response.json().builders;",
                  "if (builders.length) pm.collectionVariables.set(\"builder_id\", builders[0].id);",
                  "",
                  "pm.test(\"marks which are the platform's\", () => {",
                  "  pm.expect(builders[0]).to.have.property(\"global\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Add a builder inline",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/builders",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "builders"
              ]
            },
            "description": "The design puts this on the project form so a broker is never blocked by a missing developer.\n\nWhat they add belongs to their firm alone \u2014 it does not join the global list, and a firm may reuse a name the platform already has.\n\nThe name carries `{{$timestamp}}` so the collection is re-runnable \u2014 builder names are unique per firm, and a fixed one would 422 on the second pass.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Sethi Preferred {{$timestamp}}\",\n  \"website\": \"https://sethipreferred.example.com\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "pm.test(\"belongs to the firm, not the platform\", () => {",
                  "  pm.expect(pm.response.json().builder.global).to.be.false;",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List buildings",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/buildings",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "buildings"
              ]
            },
            "description": "Firm-owned. `q` searches by name \u2014 this is what backs the property form's building picker."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const buildings = pm.response.json().buildings;",
                  "if (buildings.length) pm.collectionVariables.set(\"building_id\", buildings[0].id);"
                ]
              }
            }
          ]
        },
        {
          "name": "Create a building",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/buildings",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "buildings"
              ]
            },
            "description": "The design's \"Building not listed? Add new\" modal, with the city pre-filled.\n\nAmenities live here rather than on the listing \u2014 every flat in the building shares the pool. A locality outside the chosen city is rejected.\n\nName carries `{{$timestamp}}`: buildings are unique per firm and locality, so a fixed name would 422 on a re-run.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Aurum Heights {{$timestamp}}\",\n  \"city_id\": \"{{city_id}}\",\n  \"locality_id\": \"{{locality_id}}\",\n  \"address\": \"Plot 14, Kolshet Road\",\n  \"lat\": 19.219,\n  \"lng\": 72.9772,\n  \"google_place_id\": \"ChIJb8Jg9pZ_XjkRQvDBGgAAAAC\",\n  \"has_pool\": true,\n  \"has_gym\": false\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Pull real master ids so bodies don't hardcode uuids.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/reference\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 200) { return; }",
                  "  const ref = res.json();",
                  "  const city = ref.cities.find((c) => c.name === \"Navi Mumbai\") || ref.cities[0];",
                  "  const locality = ref.localities.find((l) => l.city_id === city.id) || ref.localities[0];",
                  "  pm.collectionVariables.set(\"city_id\", city.id);",
                  "  pm.collectionVariables.set(\"locality_id\", locality.id);",
                  "  pm.collectionVariables.set(\"builder_id\", ref.builders[0].id);",
                  "  pm.collectionVariables.set(\"typology_id\", (ref.typologies.find((t) => t.name === \"2 BHK\") || ref.typologies[0]).id);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "pm.collectionVariables.set(\"building_id\", pm.response.json().building.id);"
                ]
              }
            }
          ]
        },
        {
          "name": "Update a building",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/buildings/{{building_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "buildings",
                "{{building_id}}"
              ]
            },
            "description": "Amenities live on the **building**, not the listing \u2014 every flat in it shares the same pool, so setting `has_pool` here changes what every property in the building reports.\n\nBuildings are firm-owned and unique on `(firm_id, name, locality_id)`: renaming one onto another building's name in the same locality is a 422.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Aurum Heights {{$timestamp}}\",\n  \"city_id\": \"{{city_id}}\",\n  \"locality_id\": \"{{locality_id}}\",\n  \"address\": \"Off Linking Road\",\n  \"lat\": 19.2195,\n  \"lng\": 72.9768,\n  \"google_place_id\": \"ChIJb8Jg9pZ_XjkRQvDBGgAAAAD\",\n  \"has_pool\": true,\n  \"has_gym\": true\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const b = pm.response.json().building;",
                  "pm.test(\"amenity flipped\", () => pm.expect(b.has_gym).to.eql(true));"
                ]
              }
            }
          ]
        },
        {
          "name": "List projects",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects"
              ]
            },
            "description": "My Projects only (`source: own`). Marketplace is an empty client pill \u2014 do not list catalog here.\n\nFilters: `q` (dropped if any drawer param is set; `status` does not drop `q`), `builder_id`, `city_id`, `locality_id`, `possession_before`, `budget_min`, `budget_max`, `brokerage_min`, `brokerage_max` (NULL brokerage drops out), `typology_ids[]`, `status` (`active` default \u00b7 `archived` \u00b7 `all`).\n\nList/detail include `avg_psf` (unweighted mean of config rates). **Budget filtering matches a project if *any* of its typologies starts in the window.**"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const projects = pm.response.json().projects;",
                  "if (projects.length) pm.collectionVariables.set(\"project_id\", projects[0].id);",
                  "",
                  "pm.test(\"carries derived bands\", () => {",
                  "  const withTypologies = projects.find((p) => p.price_band);",
                  "  if (withTypologies) {",
                  "    pm.expect(withTypologies.price_band).to.have.property(\"from\");",
                  "    pm.expect(withTypologies.area_band).to.have.property(\"to\");",
                  "  }",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List \u2014 every filter",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects?status=active&builder_id={{builder_id}}&city_id={{city_id}}&locality_id={{locality_id}}&budget_min=5000000&budget_max=50000000&brokerage_min=1&brokerage_max=10&possession_before=2029-12-31&typology_ids[]={{typology_id}}&per_page=5&page=1&sort=recent",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects"
              ],
              "query": [
                {
                  "key": "status",
                  "value": "active"
                },
                {
                  "key": "builder_id",
                  "value": "{{builder_id}}"
                },
                {
                  "key": "city_id",
                  "value": "{{city_id}}"
                },
                {
                  "key": "locality_id",
                  "value": "{{locality_id}}"
                },
                {
                  "key": "budget_min",
                  "value": "5000000"
                },
                {
                  "key": "budget_max",
                  "value": "50000000"
                },
                {
                  "key": "brokerage_min",
                  "value": "1"
                },
                {
                  "key": "brokerage_max",
                  "value": "10"
                },
                {
                  "key": "possession_before",
                  "value": "2029-12-31"
                },
                {
                  "key": "typology_ids[]",
                  "value": "{{typology_id}}"
                },
                {
                  "key": "per_page",
                  "value": "5"
                },
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "sort",
                  "value": "recent"
                }
              ]
            },
            "description": "Every filter on the projects index. `status` is `active` or `archived` \u2014 there is no destroy anywhere in inventory, archiving is the status change. Drawer params drop `q`; the Active pill does not. `brokerage_min`/`brokerage_max` skip projects with NULL brokerage. List rows include `avg_psf`."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "// Filters legitimately narrow to zero rows. What must hold is that every",
                  "// parameter was understood \u2014 an unknown one is ignored in Rails, so a 200",
                  "// with sane pagination is the honest assertion here.",
                  "pm.test(\"pagination meta comes back\", () => {",
                  "  pm.expect(body.meta.page).to.eql(1);",
                  "  pm.expect(body.meta.per_page).to.eql(5);",
                  "  pm.expect(body.meta.total_count).to.be.a(\"number\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Create a project",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/projects",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects"
              ]
            },
            "description": "Project and typologies in one call \u2014 a project saved without them shows an empty price band, which is worse than no project.\n\n`possession_on` may be replaced by `possession_label` (\"Ready\") for completed projects; one of the two is required. Attach a brochure with `brochure_signed_id` from `/uploads`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Aurum Vista {{$timestamp}}\",\n  \"builder_id\": \"{{builder_id}}\",\n  \"city_id\": \"{{city_id}}\",\n  \"locality_id\": \"{{locality_id}}\",\n  \"address\": \"Off Ghodbunder Road, Kolshet\",\n  \"lat\": 19.2183,\n  \"lng\": 72.9781,\n  \"google_place_id\": \"ChIJb8Jg9pZ_XjkRQvDBGgAAAAA\",\n  \"starting_budget\": 14200000,\n  \"possession_on\": \"2027-12-31\",\n  \"possession_label\": \"Dec 2027\",\n  \"rera_number\": \"P51700054321\",\n  \"brokerage_percent\": 2.5,\n  \"promo_text\": \"Launch pricing till month end\",\n  \"promo_ends_on\": \"2027-12-31\",\n  \"status\": \"active\",\n  \"typologies\": [\n    {\n      \"typology_id\": \"{{typology_id}}\",\n      \"starting_price\": 14200000,\n      \"starting_carpet_sqft\": 720\n    }\n  ],\n  \"brochure_signed_id\": \"{{brochure_signed_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// A project may carry a brochure \u2014 ticket, then PUT the file, then send the",
                  "// signed_id with the project itself. Same three steps as a photo.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\", method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"project_brochure\", filename: \"aurum-vista.pdf\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"application/pdf\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const t = res.json();",
                  "  pm.collectionVariables.set(\"brochure_signed_id\", t.signed_id);",
                  "  pm.sendRequest({ url: t.direct_upload.url, method: \"PUT\",",
                  "    header: t.direct_upload.headers, body: { mode: \"raw\", raw: \"hello world\" } }, () => {});",
                  "});",
                  "",
                  "// Pull real master ids so bodies don't hardcode uuids.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/reference\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 200) { return; }",
                  "  const ref = res.json();",
                  "  const city = ref.cities.find((c) => c.name === \"Navi Mumbai\") || ref.cities[0];",
                  "  const locality = ref.localities.find((l) => l.city_id === city.id) || ref.localities[0];",
                  "  pm.collectionVariables.set(\"city_id\", city.id);",
                  "  pm.collectionVariables.set(\"locality_id\", locality.id);",
                  "  pm.collectionVariables.set(\"builder_id\", ref.builders[0].id);",
                  "  pm.collectionVariables.set(\"typology_id\", (ref.typologies.find((t) => t.name === \"2 BHK\") || ref.typologies[0]).id);",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const project = pm.response.json().project;",
                  "pm.collectionVariables.set(\"project_id\", project.id);",
                  "",
                  "pm.test(\"band derives from the typologies\", () => {",
                  "  pm.expect(project.price_band.from).to.eql(14200000);",
                  "});",
                  "",
                  "pm.test(\"created as the firm's own, not catalog\", () => {",
                  "  pm.expect(project.source).to.eql(\"own\");",
                  "});",
                  "",
                  "pm.test(\"the brochure is attached\", () => pm.expect(project.brochure_url).to.be.a(\"string\"));"
                ]
              }
            }
          ]
        },
        {
          "name": "Get a project",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects/{{project_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "{{project_id}}"
              ]
            },
            "description": "Detail adds the typology table with a rate per sqft each, the brochure, photo urls, and a **`shareable`** subset.\n\n`shareable` deliberately omits `brokerage_percent` \u2014 what the broker earns is not the client's business. Build share messages from `shareable` rather than the top-level object."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const project = pm.response.json().project;",
                  "",
                  "pm.test(\"shareable hides what the broker earns\", () => {",
                  "  pm.expect(project).to.have.property(\"brokerage_percent\");",
                  "  pm.expect(project.shareable).to.not.have.property(\"brokerage_percent\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Update a project",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/projects/{{project_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "{{project_id}}"
              ]
            },
            "description": "Send only what changed. Omitting `typologies` leaves them alone; sending the key at all **replaces the whole set**, so a partial list silently drops the rest.\n\nPrice and area bands are derived from the typologies on read, never stored, so they move on their own once a typology changes.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Aurum Vista Phase 2 {{$timestamp}}\",\n  \"builder_id\": \"{{builder_id}}\",\n  \"city_id\": \"{{city_id}}\",\n  \"locality_id\": \"{{locality_id}}\",\n  \"address\": \"Off Ghodbunder Road, Kolshet West\",\n  \"lat\": 19.2201,\n  \"lng\": 72.976,\n  \"google_place_id\": \"ChIJb8Jg9pZ_XjkRQvDBGgAAAAB\",\n  \"starting_budget\": 15100000,\n  \"possession_on\": \"2028-03-31\",\n  \"possession_label\": \"Dec 2027\",\n  \"rera_number\": \"P51700054322\",\n  \"brokerage_percent\": 2.75,\n  \"promo_text\": \"Launch pricing till month end\",\n  \"promo_ends_on\": \"2027-12-31\",\n  \"status\": \"active\",\n  \"typologies\": [\n    {\n      \"typology_id\": \"{{typology_id}}\",\n      \"starting_price\": 15100000,\n      \"starting_carpet_sqft\": 760\n    }\n  ],\n  \"brochure_signed_id\": \"{{brochure_signed_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// A project may carry a brochure \u2014 ticket, then PUT the file, then send the",
                  "// signed_id with the project itself. Same three steps as a photo.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\", method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"project_brochure\", filename: \"aurum-vista.pdf\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"application/pdf\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const t = res.json();",
                  "  pm.collectionVariables.set(\"brochure_signed_id\", t.signed_id);",
                  "  pm.sendRequest({ url: t.direct_upload.url, method: \"PUT\",",
                  "    header: t.direct_upload.headers, body: { mode: \"raw\", raw: \"hello world\" } }, () => {});",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const project = pm.response.json().project;",
                  "",
                  "",
                  "// `promo` is an object and appears only while the promo is live \u2014 an expired",
                  "// one is omitted entirely rather than sent with a past date for the client to",
                  "// check. That is why the body sets promo_ends_on too.",
                  "pm.test(\"the promo is live and carries the new text\", () => {",
                  "  pm.expect(project.promo).to.be.an(\"object\");",
                  "  pm.expect(project.promo.text).to.eql(\"Launch pricing till month end\");",
                  "});",
                  "pm.test(\"brokerage is a number, not a string\", () => {",
                  "  pm.expect(project.brokerage_percent).to.eql(2.75);",
                  "});",
                  "",
                  "pm.test(\"the brochure is attached\", () => pm.expect(project.brochure_url).to.be.a(\"string\"));"
                ]
              }
            }
          ]
        },
        {
          "name": "List projects \u2014 newest first",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects?sort=recent",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects"
              ],
              "query": [
                {
                  "key": "sort",
                  "value": "recent"
                }
              ]
            },
            "description": "`sort=recent` puts a just-created project on page 1. The default, `sort=name`, is A\u2013Z \u2014 which is why a new project can land on page 2 and look missing."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const list = pm.response.json().projects;",
                  "pm.test(\"the project created above is first\", () => {",
                  "  pm.expect(list[0].id).to.eql(pm.collectionVariables.get(\"project_id\"));",
                  "});",
                  "pm.test(\"and the rest are newest first\", () => {",
                  "  const times = list.map(p => Date.parse(p.created_at));",
                  "  pm.expect(times).to.eql([...times].sort((a, b) => b - a));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Search projects \u2014 typeahead",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects/search?q=aurum",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "search"
              ],
              "query": [
                {
                  "key": "q",
                  "value": "aurum"
                }
              ]
            },
            "description": "Typeahead for a search box: call once **3+ characters** are typed, debounced.\n\nTypo-tolerant on the name, exact fragments on the RERA number. Ranked exact \u2192 prefix \u2192 contains \u2192 typo, capped at 10. `meta.more` is true when there are more than 10.\n\nA hit carries only what a row shows \u2014 tap through to `GET /projects/:id` for the rest."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "pm.test(\"finds the project created above\", () => {",
                  "  pm.expect(body.projects.map(p => p.id)).to.include(pm.collectionVariables.get(\"project_id\"));",
                  "});",
                  "pm.test(\"literal matches, so not a did-you-mean\", () => pm.expect(body.meta.fuzzy).to.eql(false));",
                  "pm.test(\"at most ten, with the rules in meta\", () => {",
                  "  pm.expect(body.projects.length).to.be.at.most(10);",
                  "  pm.expect(body.meta).to.include({ limit: 10, min_length: 3, query: \"aurum\" });",
                  "  pm.expect(body.meta.more).to.be.a(\"boolean\");",
                  "});",
                  "pm.test(\"a hit is slim \u2014 no price bands, photos or brokerage\", () => {",
                  "  pm.expect(Object.keys(body.projects[0]).sort())",
                  "    .to.eql([\"builder\", \"city\", \"id\", \"locality\", \"name\", \"rera_number\", \"source\"]);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Search tolerates a typo",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects/search?q=aurm",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "search"
              ],
              "query": [
                {
                  "key": "q",
                  "value": "aurm"
                }
              ]
            },
            "description": "`aurm` matches nothing as typed, so the search falls back to close spellings and finds *Aurum Vista*. `meta.fuzzy` is `true` so the app can label it \"did you mean\"."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test(\"the misspelling still finds Aurum\", () => {",
                  "  pm.expect(body.projects.map(p => p.name).some(n => /^Aurum/.test(n))).to.eql(true);",
                  "});",
                  "pm.test(\"and says these are close spellings\", () => pm.expect(body.meta.fuzzy).to.eql(true));"
                ]
              }
            }
          ]
        },
        {
          "name": "Search needs three characters",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects/search?q=au",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "search"
              ],
              "query": [
                {
                  "key": "q",
                  "value": "au"
                }
              ]
            },
            "description": "Fewer than three characters is a `422 query_too_short`, with the minimum in `details` so the app never hardcodes it."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 query_too_short\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"query_too_short\");",
                  "});",
                  "pm.test(\"says how many it needs\", () => {",
                  "  pm.expect(pm.response.json().error.details).to.eql({ min_length: 3, length: 2 });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Add project photos",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/projects/{{project_id}}/photos",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "{{project_id}}",
                "photos"
              ]
            },
            "description": "Photos live on the detail screen, not the create form.\n\nThree steps: `POST /uploads` for a ticket, PUT the file to the returned url, then send the `signed_id` here. The pre-request script does the first two, so this request stands alone.\n\n**Note:** there is no matching request for `DELETE /projects/:id/photos/:photo_id` because no response returns an attachment id \u2014 only `photo_urls`. The endpoint exists but no client can address it. See the README.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"photo_signed_ids\": [\n    \"{{signed_id}}\"\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Ticket, then PUT the file \u2014 what a client does before attaching.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\",",
                  "  method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"project_photo\", filename: \"tower.png\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"image/png\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const ticket = res.json();",
                  "  pm.collectionVariables.set(\"signed_id\", ticket.signed_id);",
                  "",
                  "  pm.sendRequest({",
                  "    url: ticket.direct_upload.url,",
                  "    method: \"PUT\",",
                  "    header: ticket.direct_upload.headers,",
                  "    body: { mode: \"raw\", raw: \"hello world\" }",
                  "  }, () => {});",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const p = pm.response.json().project;",
                  "pm.test(\"the photo is on the project\", () => pm.expect(p.photo_count).to.be.above(0));",
                  "pm.test(\"and a cover url comes back\", () => pm.expect(p.cover_photo_url).to.be.a(\"string\"));",
                  "",
                  "// `photos` carries the attachment id; `photo_urls` does not. The id is",
                  "// what DELETE /photos/:photo_id takes \u2014 the signed blob URL encodes the",
                  "// blob id, a different record, so it cannot be scraped from the url.",
                  "pm.collectionVariables.set(\"project_photo_id\", p.photos[p.photos.length - 1].id);",
                  "pm.test(\"each photo comes back with an addressable id\", () => {",
                  "  pm.expect(p.photos[0].id).to.be.a(\"string\");",
                  "  pm.expect(p.photos[0].url).to.be.a(\"string\");",
                  "});",
                  "pm.test(\"but the shareable payload carries no ids\", () => {",
                  "  pm.expect(p.shareable).to.not.have.property(\"photos\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Remove a project photo",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{api_base}}/projects/{{project_id}}/photos/{{project_photo_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "projects",
                "{{project_id}}",
                "photos",
                "{{project_photo_id}}"
              ]
            },
            "description": "Takes the **attachment** id from `photos[].id` on the project detail.\n\nPurges the blob as well as detaching it \u2014 an unreferenced file would sit in storage being paid for forever."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const p = pm.response.json().project;",
                  "pm.test(\"the photo is gone\", () => {",
                  "  const id = pm.collectionVariables.get(\"project_photo_id\");",
                  "  pm.expect(p.photos.map(x => x.id)).to.not.include(id);",
                  "});",
                  "pm.test(\"and the count follows\", () => pm.expect(p.photo_count).to.eql(p.photos.length));"
                ]
              }
            }
          ]
        },
        {
          "name": "List properties",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/properties",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties"
              ]
            },
            "description": "Filters: `q` (dropped if any drawer param is set; `status` does not drop `q`), `listing_for`, `city_id`, `locality_id`, `building_id`, `typology_id`, `floor_band`, `price_min`, `price_max`, `carpet_min`, `carpet_max` (NULL carpet drops out), `status` (`available` default \u00b7 `booked` \u00b7 `sold_out` \u00b7 `all`).\n\n`price` is the sale price, or the **monthly rent** when `listing_for` is `rent` \u2014 so `rate_per_sqft` on a rental is rent per sqft per month.\n\n**`confidential_note` is never in this payload.**"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const properties = pm.response.json().properties;",
                  "if (properties.length) pm.collectionVariables.set(\"property_id\", properties[0].id);",
                  "",
                  "pm.test(\"no confidential note in a list\", () => {",
                  "  properties.forEach((p) => pm.expect(p).to.not.have.property(\"confidential_note\"));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List \u2014 every filter ",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/properties?status=available&listing_for=sale&floor_band=higher&building_id={{building_id}}&typology_id={{typology_id}}&city_id={{city_id}}&locality_id={{locality_id}}&price_min=5000000&price_max=50000000&carpet_min=400&carpet_max=2000&per_page=5&page=1",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties"
              ],
              "query": [
                {
                  "key": "status",
                  "value": "available"
                },
                {
                  "key": "listing_for",
                  "value": "sale"
                },
                {
                  "key": "floor_band",
                  "value": "higher"
                },
                {
                  "key": "building_id",
                  "value": "{{building_id}}"
                },
                {
                  "key": "typology_id",
                  "value": "{{typology_id}}"
                },
                {
                  "key": "city_id",
                  "value": "{{city_id}}"
                },
                {
                  "key": "locality_id",
                  "value": "{{locality_id}}"
                },
                {
                  "key": "price_min",
                  "value": "5000000"
                },
                {
                  "key": "price_max",
                  "value": "50000000"
                },
                {
                  "key": "carpet_min",
                  "value": "400"
                },
                {
                  "key": "carpet_max",
                  "value": "2000"
                },
                {
                  "key": "per_page",
                  "value": "5"
                },
                {
                  "key": "page",
                  "value": "1"
                }
              ]
            },
            "description": "Every filter on the properties index. `status` is `available` / `booked` / `sold_out`, `floor_band` is `lower` / `middle` / `higher`, `listing_for` is `sale` / `rent`. Drawer params drop `q`. `carpet_min`/`carpet_max` skip listings with no carpet.\n\nNo list payload carries `confidential_note`, whatever the filter."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "// Filters legitimately narrow to zero rows. What must hold is that every",
                  "// parameter was understood \u2014 an unknown one is ignored in Rails, so a 200",
                  "// with sane pagination is the honest assertion here.",
                  "pm.test(\"pagination meta comes back\", () => {",
                  "  pm.expect(body.meta.page).to.eql(1);",
                  "  pm.expect(body.meta.per_page).to.eql(5);",
                  "  pm.expect(body.meta.total_count).to.be.a(\"number\");",
                  "});",
                  "pm.test(\"no confidential note leaks into a filtered list\", () => {",
                  "  pm.expect(body.properties.every(x => !(\"confidential_note\" in x))).to.eql(true);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Create a property",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/properties",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties"
              ]
            },
            "description": "Every accepted field. `status` is `available` / `booked` / `sold_out`; `floor_band` is `lower` / `middle` / `higher`; `listing_for` is `sale` / `rent`. Mark sold = PATCH `sold_out` (reversible).\n\n`confidential_note` is returned only by the detail \u2014 never in a list and never in `shareable`.\n\n`rate_per_sqft` is derived from price and carpet area, never sent. On a rental `price` is monthly rent, so the rate is rent per sqft per month.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"building_id\": \"{{building_id}}\",\n  \"typology_id\": \"{{typology_id}}\",\n  \"listing_for\": \"sale\",\n  \"floor_band\": \"higher\",\n  \"price\": 11800000,\n  \"carpet_area_sqft\": 690,\n  \"available_from\": \"2026-10-01\",\n  \"description\": \"North-east facing, two covered parkings.\",\n  \"confidential_note\": \"Owner will close at 1.15 Cr. Do not share.\",\n  \"status\": \"available\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const property = pm.response.json().property;",
                  "pm.collectionVariables.set(\"property_id\", property.id);",
                  "",
                  "pm.test(\"derives the title and the rate\", () => {",
                  "  pm.expect(property.title).to.be.a(\"string\");",
                  "  pm.expect(property.rate_per_sqft).to.eql(17101);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get a property",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/properties/{{property_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties",
                "{{property_id}}"
              ]
            },
            "description": "The only response that carries `confidential_note`. `shareable` holds exactly what may go to a client \u2014 build share text from it and the note cannot leak by accident."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const property = pm.response.json().property;",
                  "",
                  "pm.test(\"detail carries the note\", () => {",
                  "  pm.expect(property).to.have.property(\"confidential_note\");",
                  "});",
                  "",
                  "pm.test(\"shareable does not\", () => {",
                  "  pm.expect(property.shareable).to.not.have.property(\"confidential_note\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Update a property",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/properties/{{property_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties",
                "{{property_id}}"
              ]
            },
            "description": "`price` is monthly rent on a rental and the whole ask on a sale, so `rate_per_sqft` \u2014 derived, never stored \u2014 means rent per sqft per month on one and price per sqft on the other.\n\n`confidential_note` can be written here and comes back only on the detail; it is absent from the list payload and from `shareable`, which is what makes composing a client message from `shareable` safe.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"building_id\": \"{{building_id}}\",\n  \"typology_id\": \"{{typology_id}}\",\n  \"listing_for\": \"sale\",\n  \"floor_band\": \"middle\",\n  \"price\": 12400000,\n  \"carpet_area_sqft\": 690,\n  \"available_from\": \"2026-11-01\",\n  \"description\": \"North-east facing, two covered parkings. Recently painted.\",\n  \"confidential_note\": \"Owner will close at 1.21 Cr\",\n  \"status\": \"booked\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const p = pm.response.json().property;",
                  "pm.test(\"price updated\", () => pm.expect(p.price).to.eql(12400000));",
                  "pm.test(\"rate per sqft follows the new price\", () => {",
                  "  pm.expect(Math.round(p.rate_per_sqft)).to.eql(Math.round(12400000 / p.carpet_area_sqft));",
                  "});",
                  "pm.test(\"the confidential note never reaches shareable\", () => {",
                  "  pm.expect(p.shareable).to.not.have.property(\"confidential_note\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Attach a photo",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/properties/{{property_id}}/photos",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties",
                "{{property_id}}",
                "photos"
              ]
            },
            "description": "Photos live on the detail screen, not the create form \u2014 the design is explicit about that.\n\nThree steps: `POST /uploads` for a ticket, PUT the file to the returned url, then send the `signed_id` here. Attaching before the PUT lands returns `upload_incomplete`. A ticket from another firm, or issued for a different purpose, is `invalid_upload`.\n\nThe same attach check applies to brochures, booking documents and collection proofs.\n\nThe pre-request script does the first two steps, so this request works on its own.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"photo_signed_ids\": [\n    \"{{signed_id}}\"\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Ticket, then PUT the file \u2014 what a client does before attaching.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\",",
                  "  method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"property_photo\", filename: \"flat.txt\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"image/png\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const ticket = res.json();",
                  "  pm.collectionVariables.set(\"signed_id\", ticket.signed_id);",
                  "",
                  "  pm.sendRequest({",
                  "    url: ticket.direct_upload.url,",
                  "    method: \"PUT\",",
                  "    header: ticket.direct_upload.headers,",
                  "    body: { mode: \"raw\", raw: \"hello world\" }",
                  "  }, () => {});",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "pm.test(\"photo is attached and becomes the cover\", () => {",
                  "  const property = pm.response.json().property;",
                  "  pm.expect(property.photo_count).to.be.above(0);",
                  "  pm.expect(property.cover_photo_url).to.be.a(\"string\");",
                  "});",
                  "",
                  "const p = pm.response.json().property;",
                  "",
                  "// `photos` carries the attachment id; `photo_urls` does not. The id is",
                  "// what DELETE /photos/:photo_id takes \u2014 the signed blob URL encodes the",
                  "// blob id, a different record, so it cannot be scraped from the url.",
                  "pm.collectionVariables.set(\"property_photo_id\", p.photos[p.photos.length - 1].id);",
                  "pm.test(\"each photo comes back with an addressable id\", () => {",
                  "  pm.expect(p.photos[0].id).to.be.a(\"string\");",
                  "  pm.expect(p.photos[0].url).to.be.a(\"string\");",
                  "});",
                  "pm.test(\"but the shareable payload carries no ids\", () => {",
                  "  pm.expect(p.shareable).to.not.have.property(\"photos\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Remove a property photo",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{api_base}}/properties/{{property_id}}/photos/{{property_photo_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "properties",
                "{{property_id}}",
                "photos",
                "{{property_photo_id}}"
              ]
            },
            "description": "Same as the project one. A photo id that isn't on this property is a 404, not a 403 \u2014 consistent with how every other not-yours lookup answers."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const p = pm.response.json().property;",
                  "pm.test(\"the photo is gone\", () => {",
                  "  const id = pm.collectionVariables.get(\"property_photo_id\");",
                  "  pm.expect(p.photos.map(x => x.id)).to.not.include(id);",
                  "});",
                  "pm.test(\"and the count follows\", () => pm.expect(p.photo_count).to.eql(p.photos.length));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Bookings and money",
      "description": "Recording a sale and the brokerage on it.\n\n**Managers and super admins only.** An agent gets `forbidden_role` on every request in this folder, including bookings for a lead of their own.\n\n**Net income is computed server-side and stored:**\n\n```\nround(agreement_value \u00d7 commission_percent / 100) + kicker \u2212 passback\n```\n\nThe design's own example is the fixture: \u20b91,56,00,000 at 4.5% is \u20b97,02,000, plus a \u20b950,000 kicker, minus a \u20b966,000 passback = **\u20b96,86,000** \u2014 the \"\u20b96.86 L\" on its booking screen. The client never recomputes money; `revenue` carries every line already worked out.\n\n**Two hard blocks.** Invoices cannot exceed net income; collections cannot exceed what was invoiced, and a payment naming an invoice cannot exceed that invoice. Refusals carry the arithmetic in `error.details` so the app can explain itself.\n\n**Cancelling** sets the booking's status and nothing else \u2014 the lead is untouched and invoices already raised stay on record.",
      "item": [
        {
          "name": "Create a booking",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings"
              ]
            },
            "description": "Every accepted field.\n\n`unit_no` is **required when `project_id` is set**, and unique among live bookings on that project. Cancel frees the unit (`422 unit_taken` if still live).\n\nIf `project_id` is a catalog row, the server copies it into My Projects. A name clash is `422 project_name_clash` \u2014 retry with `use_existing: true` or `new_name`.\n\n`customer_name` and `customer_mobile` are **snapshots** \u2014 sent here they are stored as given; omitted, they are copied from the lead. Correcting the lead a year later must not rewrite what was booked.\n\n`net_income` is computed and stored, never sent:\n\n    round(agreement_value x commission_percent / 100) + kicker - passback\n\n15600000 at 4.5% is 702000, +50000 kicker, -66000 passback = **686000**.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lead_id\": \"{{lead_id}}\",\n  \"project_id\": \"{{project_id}}\",\n  \"builder_ref_no\": \"AV/BK/1184\",\n  \"unit_no\": \"B-1104\",\n  \"carpet_area_sqft\": 1340,\n  \"customer_name\": \"Rhea Kapoor\",\n  \"customer_mobile\": \"+919820166666\",\n  \"booked_on\": \"2026-08-01\",\n  \"agreement_value\": 15600000,\n  \"commission_percent\": 4.5,\n  \"kicker\": 50000,\n  \"passback\": 66000,\n  \"other_details\": \"10:80:10 plan \u00b7 two covered parkings\",\n  \"registration_done_on\": \"2026-08-20\",\n  \"client_paid_percent\": 10\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Grab a lead to book against.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/leads\"),",
                  "  method: \"GET\",",
                  "  header: { Authorization: \"Bearer \" + pm.variables.replaceIn(\"{{access_token}}\") }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200 && res.json().leads.length) {",
                  "    pm.collectionVariables.set(\"lead_id\", res.json().leads[0].id);",
                  "  }",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const b = pm.response.json().booking;",
                  "pm.collectionVariables.set(\"booking_id\", b.id);",
                  "",
                  "pm.test(\"net income matches the design's worked example\", () => {",
                  "  pm.expect(b.revenue.commission_amount).to.eql(702000);",
                  "  pm.expect(b.revenue.net_income).to.eql(686000);",
                  "});",
                  "",
                  "pm.test(\"gets a per-firm code\", () => pm.expect(b.code).to.match(/^B-\\d{4,}$/));"
                ]
              }
            }
          ]
        },
        {
          "name": "List bookings",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings"
              ]
            },
            "description": "Cancelled bookings are excluded unless you ask for them \u2014 the design says cancelling \"removes this booking from revenue\".\n\nFilters: `q` (customer, unit, code, builder ref, project name), `client_phone`, `project_id`, `booked_from`, `booked_to`, `status` (`live` default \u00b7 `cancelled`).\n\n`totals` carries the firm's live agreement value and net income for the header."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "pm.test(\"carries live totals\", () => {",
                  "  pm.expect(pm.response.json().totals).to.have.property(\"net_income\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List \u2014 every filter",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings?q=Rhea&status=live&client_phone=9820166666&project_id={{project_id}}&booked_from=2026-01-01&booked_to=2027-12-31&per_page=5&page=1",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings"
              ],
              "query": [
                {
                  "key": "q",
                  "value": "Rhea"
                },
                {
                  "key": "status",
                  "value": "live"
                },
                {
                  "key": "client_phone",
                  "value": "9820166666"
                },
                {
                  "key": "project_id",
                  "value": "{{project_id}}"
                },
                {
                  "key": "booked_from",
                  "value": "2026-01-01"
                },
                {
                  "key": "booked_to",
                  "value": "2027-12-31"
                },
                {
                  "key": "per_page",
                  "value": "5"
                },
                {
                  "key": "page",
                  "value": "1"
                }
              ]
            },
            "description": "Every filter on the bookings index. `status` is `live` or `cancelled`; every money query starts from `live`.\n\n`client_phone` matches the **snapshot** on the booking, not the lead's current number.\n\nThe `totals` block is computed over the filtered set by re-selecting on id \u2014 never over a scope carrying `includes(:invoices, :collections)`, which becomes a LEFT JOIN and counts a booking once per associated row. That bug reported 3.12 Cr against a true 1.56 Cr."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "// Filters legitimately narrow to zero rows. What must hold is that every",
                  "// parameter was understood \u2014 an unknown one is ignored in Rails, so a 200",
                  "// with sane pagination is the honest assertion here.",
                  "pm.test(\"pagination meta comes back\", () => {",
                  "  pm.expect(body.meta.page).to.eql(1);",
                  "  pm.expect(body.meta.per_page).to.eql(5);",
                  "  pm.expect(body.meta.total_count).to.be.a(\"number\");",
                  "});",
                  "pm.test(\"totals are present and non-negative\", () => {",
                  "  pm.expect(body.totals.net_income).to.be.at.least(0);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Get a booking",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}"
              ]
            },
            "description": "The detail the design's tabs are built from: the itemised `revenue` block, documents, invoices and collections, plus `invoiced` / `collected` / `outstanding`."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));"
                ]
              }
            }
          ]
        },
        {
          "name": "Update a booking",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}"
              ]
            },
            "description": "Net income is recomputed and **stored** on every save:\n\n    round(agreement_value x commission_percent / 100) + kicker - passback\n\nBigDecimal, half-up, rounded once where the percentage meets the value. Stored rather than derived so a booking's displayed revenue can never drift from what the reports sum.\n\nA cancelled booking refuses updates \u2014 `booking_cancelled`.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"lead_id\": \"{{lead_id}}\",\n  \"project_id\": \"{{project_id}}\",\n  \"builder_ref_no\": \"AV/BK/1184-R1\",\n  \"unit_no\": \"B-1204\",\n  \"carpet_area_sqft\": 1360,\n  \"customer_name\": \"Rhea Kapoor\",\n  \"customer_mobile\": \"+919820166666\",\n  \"booked_on\": \"2026-08-01\",\n  \"agreement_value\": 15600000,\n  \"commission_percent\": 5.0,\n  \"kicker\": 50000,\n  \"passback\": 66000,\n  \"other_details\": \"10:80:10 plan \u00b7 two covered parkings \u00b7 corner unit\",\n  \"registration_done_on\": \"2026-08-25\",\n  \"client_paid_percent\": 20\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const b = pm.response.json().booking;",
                  "",
                  "// The itemised money lives under `revenue` \u2014 the design's screen breaks it",
                  "// down that way so the client never recomputes it.",
                  "pm.test(\"net income was recomputed, not left stale\", () => {",
                  "  const r = b.revenue;",
                  "  const expected = Math.round(b.agreement_value * 5.0 / 100) + r.kicker - r.passback;",
                  "  pm.expect(r.net_income).to.eql(expected);",
                  "  pm.expect(b.net_income).to.eql(expected);",
                  "});",
                  "pm.test(\"commission percent came back as sent\", () => {",
                  "  pm.expect(b.revenue.commission_percent).to.eql(5);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Raise an invoice",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/invoices",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "invoices"
              ]
            },
            "description": "The number is **entered by the broker**, not generated, so it matches the invoice they actually raised. Unique per firm.\n\nCapped at net income \u2014 see the next request.\n\nThe number carries `{{$timestamp}}` because it is unique per firm. A fixed one 422s on the second run \u2014 and because each run creates a fresh booking, that left the next request with nothing invoiced and the over-invoicing check passed when it should have failed. Exactly the cascade running the collection is meant to catch.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"number\": \"INV-{{$timestamp}}\",\n  \"issued_on\": \"2026-08-04\",\n  \"amount\": 686000,\n  \"comment\": \"full brokerage\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const body = pm.response.json();",
                  "pm.collectionVariables.set(\"invoice_id\", body.invoice.id);",
                  "",
                  "pm.test(\"the booking now shows it as invoiced\", () => {",
                  "  pm.expect(body.booking.invoiced).to.eql(686000);",
                  "  pm.expect(body.booking.outstanding).to.eql(686000);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List invoices",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/invoices",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "invoices"
              ]
            },
            "description": "Everything raised against this booking, with what is still outstanding on each.\n\nInvoice numbers are entered by the broker (`INV-2026-041`) so they match what was raised outside the system, and are unique per firm. Cancelling an invoice is not built \u2014 the design shows no control for it."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "pm.test(\"the invoice just raised is here\", () => pm.expect(body.invoices.length).to.be.above(0));",
                  "",
                  "// `totals` is the server's arithmetic, not the client's. Checking the rows",
                  "// sum to it is what would catch a serializer drifting from the model.",
                  "pm.test(\"the rows sum to the invoiced total\", () => {",
                  "  const summed = body.invoices.reduce((s, i) => s + i.amount, 0);",
                  "  pm.expect(summed).to.eql(body.totals.invoiced);",
                  "});",
                  "pm.test(\"invoiced never exceeds net income \u2014 the hard block holds\", () => {",
                  "  pm.expect(body.totals.invoiced).to.be.at.most(body.totals.net_income);",
                  "  pm.expect(body.totals.invoiceable_balance).to.eql(body.totals.net_income - body.totals.invoiced);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Over-invoicing is refused",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/invoices",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "invoices"
              ]
            },
            "description": "A firm cannot invoice more than it earned. The refusal carries `net_income`, `already_invoiced`, `attempted` and `available`, so the app can say how much is left rather than just refusing.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"number\": \"INV-2026-042\",\n  \"issued_on\": \"2026-08-05\",\n  \"amount\": 100000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 over_invoiced with the arithmetic\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  const e = pm.response.json().error;",
                  "  pm.expect(e.code).to.eql(\"over_invoiced\");",
                  "  pm.expect(e.details).to.have.property(\"available\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Record a collection",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/collections",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "collections"
              ]
            },
            "description": "Every accepted field. `invoice_id` is optional \u2014 the design offers \"Unlinked payment\" as a first-class choice \u2014 but naming one is what lets the API catch a payment filed against the wrong invoice (`over_collected_for_invoice`).\n\n`mode` is `neft_rtgs` / `upi` / `cheque` / `cash`. `proof_signed_id` attaches proof of payment; the pre-request script mints the upload ticket and PUTs the file.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"invoice_id\": \"{{invoice_id}}\",\n  \"received_on\": \"2026-08-05\",\n  \"amount\": 400000,\n  \"mode\": \"neft_rtgs\",\n  \"transaction_no\": \"NEFT/2026/884213\",\n  \"proof_signed_id\": \"{{signed_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// A collection may carry proof of payment \u2014 ticket, then PUT the file.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\", method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"collection_proof\", filename: \"neft.pdf\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"application/pdf\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const t = res.json();",
                  "  pm.collectionVariables.set(\"signed_id\", t.signed_id);",
                  "  pm.sendRequest({ url: t.direct_upload.url, method: \"PUT\",",
                  "    header: t.direct_upload.headers, body: { mode: \"raw\", raw: \"hello world\" } }, () => {});",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "// Read the amount out of the request rather than hardcoding it \u2014 an earlier",
                  "// version pinned 400000 here, so editing the body broke an assertion that",
                  "// had nothing to do with the change.",
                  "const sent = JSON.parse(pm.request.body.raw).amount;",
                  "const b = pm.response.json().booking;",
                  "",
                  "pm.test(\"the collection is counted\", () => pm.expect(b.collected).to.eql(sent));",
                  "pm.test(\"outstanding is invoiced minus collected\", () => {",
                  "  pm.expect(b.outstanding).to.eql(b.invoiced - b.collected);",
                  "});",
                  "pm.test(\"and never more than what was invoiced\", () => {",
                  "  pm.expect(b.collected).to.be.at.most(b.invoiced);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "List collections",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/collections",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "collections"
              ]
            },
            "description": "Money actually received. A collection may name an invoice or stand alone \u2014 the design offers \"Unlinked payment\" as a first-class choice."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const body = pm.response.json();",
                  "pm.test(\"the collection just recorded is here\", () => {",
                  "  pm.expect(body.collections.length).to.be.above(0);",
                  "});",
                  "pm.test(\"the rows sum to the collected total\", () => {",
                  "  const summed = body.collections.reduce((s, x) => s + x.amount, 0);",
                  "  pm.expect(summed).to.eql(body.totals.collected);",
                  "});",
                  "pm.test(\"collected never exceeds invoiced, and outstanding is the difference\", () => {",
                  "  pm.expect(body.totals.collected).to.be.at.most(body.totals.invoiced);",
                  "  pm.expect(body.totals.outstanding).to.eql(body.totals.invoiced - body.totals.collected);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Attach a booking document",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/documents",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "documents"
              ]
            },
            "description": "Four slots from the design: `application_form`, `tagging_confirmation`, `lead_source_proof`, `other`. Only **`other`** may hold more than one file \u2014 a second file in a named slot is `slot_taken` rather than a silent overwrite.\n\n`{{$timestamp}}` in the label keeps re-runs distinguishable.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"slot\": \"other\",\n  \"label\": \"Allotment letter {{$timestamp}}\",\n  \"signed_id\": \"{{signed_id}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Ticket, then PUT the file.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const token = pm.variables.replaceIn(\"{{access_token}}\");",
                  "",
                  "pm.sendRequest({",
                  "  url: api + \"/uploads\",",
                  "  method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\", Authorization: \"Bearer \" + token },",
                  "  body: { mode: \"raw\", raw: JSON.stringify({",
                  "    purpose: \"booking_document\", filename: \"allotment.pdf\", byte_size: 11,",
                  "    checksum: \"XrY7u+Ae7tCTyyK7j1rNww==\", content_type: \"application/pdf\"",
                  "  }) }",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 201) { return; }",
                  "  const ticket = res.json();",
                  "  pm.collectionVariables.set(\"signed_id\", ticket.signed_id);",
                  "",
                  "  pm.sendRequest({",
                  "    url: ticket.direct_upload.url,",
                  "    method: \"PUT\",",
                  "    header: ticket.direct_upload.headers,",
                  "    body: { mode: \"raw\", raw: \"hello world\" }",
                  "  }, () => {});",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"201 Created\", () => pm.response.to.have.status(201));",
                  "",
                  "const docs = pm.response.json().booking.documents;",
                  "pm.test(\"the document is on the booking\", () => pm.expect(docs.length).to.be.above(0));",
                  "",
                  "// Unlike photos, documents come back with an id \u2014 which is what makes",
                  "// the DELETE below reachable at all.",
                  "pm.collectionVariables.set(\"document_id\", docs[docs.length - 1].id);",
                  "pm.test(\"it carries an id the client can address\", () => {",
                  "  pm.expect(docs[docs.length - 1].id).to.be.a(\"string\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Remove a booking document",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/documents/{{document_id}}",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "documents",
                "{{document_id}}"
              ]
            },
            "description": "Purges the blob as well as the row \u2014 an unreferenced file would sit in storage being paid for forever."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "",
                  "const docs = pm.response.json().booking.documents;",
                  "pm.test(\"it is gone from the booking\", () => {",
                  "  const id = pm.collectionVariables.get(\"document_id\");",
                  "  pm.expect(docs.map(d => d.id)).to.not.include(id);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Over-collecting is refused",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/collections",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "collections"
              ]
            },
            "description": "Collected can never exceed invoiced. A payment naming a specific invoice is checked twice \u2014 against the booking's total and against that invoice's own amount, which is what catches a payment filed against the wrong invoice (`over_collected_for_invoice`).",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"received_on\": \"2026-08-19\",\n  \"amount\": 99000000,\n  \"mode\": \"cash\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 over_collected\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"over_collected\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Cancelling needs a reason",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/bookings/{{booking_id}}/cancel",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "bookings",
                "{{booking_id}}",
                "cancel"
              ]
            },
            "description": "Refused deliberately. A cancelled booking with no reason is one nobody can learn from.",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"422 reason_required\", () => {",
                  "  pm.response.to.have.status(422);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"reason_required\");",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Error states",
      "description": "The blocked states the React app has to render. Each returns a typed `error.code`; two more \u2014 `account_suspended` and `subscription_lapsed` \u2014 are produced by suspending the firm or lapsing its subscription in the admin panel, then calling `Who am I`.",
      "item": [
        {
          "name": "Unregistered mobile \u2192 not_registered",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/otp",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "otp"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"mobile\": \"9000000000\"\n}"
            },
            "description": "Said plainly, because there is no self-signup: a stranger learning a number isn't registered gains nothing they can act on. The design's \"This number isn't registered\" wall."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"404 with a typed code\", () => {",
                  "  pm.response.to.have.status(404);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"not_registered\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Wrong code \u2192 invalid_code",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/verify",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"request_id\": \"{{request_id}}\",\n  \"code\": \"000000\"\n}"
            },
            "description": "Counts down `details.attempts_left`. Three wrong codes lock sign-in for 30 minutes with `otp_locked`.\n\nSelf-contained: the pre-request script asks for a fresh code first, so this works in a full collection run where `request_id` has already been consumed."
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// A verified request_id is burned, so mint a fresh one to fail against.",
                  "pm.sendRequest({",
                  "  url: pm.variables.replaceIn(\"{{api_base}}/auth/otp\"),",
                  "  method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\" },",
                  "  body: {",
                  "    mode: \"raw\",",
                  "    raw: JSON.stringify({ mobile: pm.variables.replaceIn(\"{{super_admin_mobile}}\") })",
                  "  }",
                  "}, (err, res) => {",
                  "  if (!err && res.code === 200) {",
                  "    pm.collectionVariables.set(\"request_id\", res.json().request_id);",
                  "  }",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"401 with attempts remaining\", () => {",
                  "  pm.response.to.have.status(401);",
                  "  const error = pm.response.json().error;",
                  "  pm.expect(error.code).to.eql(\"invalid_code\");",
                  "  pm.expect(error.details.attempts_left).to.be.a(\"number\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "No token \u2192 unauthorized",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/me",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "me"
              ]
            },
            "description": "Also what a revoked session returns \u2014 the session is checked on every request, so signing a device out takes effect immediately rather than when its JWT expires."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"401 unauthorized\", () => {",
                  "  pm.response.to.have.status(401);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"unauthorized\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Agent verifying a channel \u2192 forbidden_role",
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{agent_access_token}}",
                  "type": "string"
                }
              ]
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/firm/contact_channels/{{channel_id}}/request_code",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "firm",
                "contact_channels",
                "{{channel_id}}",
                "request_code"
              ]
            },
            "description": "Only the firm's `super_admin` may act on channels. Reading them still works for everyone \u2014 the split is enforced server-side, not in the client.\n\nSelf-contained: the pre-request script signs in as the agent and uses that token, so this holds in a full collection run where you are otherwise the super admin."
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Sign in as the agent, so this request is genuinely made by one.",
                  "const api = pm.variables.replaceIn(\"{{api_base}}\");",
                  "const mobile = pm.variables.replaceIn(\"{{agent_mobile}}\");",
                  "const code = pm.variables.replaceIn(\"{{otp_code}}\");",
                  "",
                  "const json = (body) => ({",
                  "  mode: \"raw\", raw: JSON.stringify(body)",
                  "});",
                  "",
                  "pm.sendRequest({",
                  "  url: api + \"/auth/otp\",",
                  "  method: \"POST\",",
                  "  header: { \"Content-Type\": \"application/json\" },",
                  "  body: json({ mobile })",
                  "}, (err, res) => {",
                  "  if (err || res.code !== 200) { return; }",
                  "",
                  "  pm.sendRequest({",
                  "    url: api + \"/auth/verify\",",
                  "    method: \"POST\",",
                  "    header: { \"Content-Type\": \"application/json\" },",
                  "    body: json({ request_id: res.json().request_id, code })",
                  "  }, (err2, res2) => {",
                  "    if (!err2 && res2.code === 200) {",
                  "      pm.collectionVariables.set(\"agent_access_token\", res2.json().access_token);",
                  "    }",
                  "  });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"403 forbidden_role when not the super admin\", () => {",
                  "  pm.response.to.have.status(403);",
                  "  pm.expect(pm.response.json().error.code).to.eql(\"forbidden_role\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Agent dashboard \u2192 no money block",
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [
                {
                  "key": "token",
                  "value": "{{agent_access_token}}",
                  "type": "string"
                }
              ]
            },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{api_base}}/dashboard",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "dashboard"
              ]
            },
            "description": "An agent gets the dashboard, minus the money. Not a 403 \u2014 the screen is legitimately theirs, just smaller \u2014 and not zeroes, which would read as \"no revenue\"."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200, not 403 \u2014 the screen is theirs\", () => pm.response.to.have.status(200));",
                  "",
                  "const b = pm.response.json();",
                  "pm.test(\"but the money block is absent, not zeroed\", () => {",
                  "  pm.expect(b).to.not.have.property(\"money\");",
                  "});",
                  "pm.test(\"the lead counters are still there\", () => pm.expect(b.leads.total).to.be.a(\"number\"));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Sign out",
      "description": "Deliberately last. Signing out revokes the session, so anything after it would get 401 \u2014 which is exactly what an earlier draft of this collection did to itself.",
      "item": [
        {
          "name": "Sign out",
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{api_base}}/auth/session",
              "host": [
                "{{api_base}}"
              ],
              "path": [
                "auth",
                "session"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refresh_token\": \"{{refresh_token}}\"\n}"
            },
            "description": "Always 204, whether or not that token was live \u2014 whether a token exists is not something an unauthenticated caller needs told.\n\nRevokes the session, so the access token stops working on the very next request rather than when it expires."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"204 No Content\", () => pm.response.to.have.status(204));"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}
