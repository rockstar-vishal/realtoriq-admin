$kind: http-request
url: "{{api_base}}/me"
method: GET
headers:
  Authorization: "{{bearer_token_1a3b}}"
scripts:
  - type: afterResponse
    code: pm.test('401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 3000
